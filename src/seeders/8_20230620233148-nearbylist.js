"use strict";

/** @type {import('sequelize-cli').Migration} */
module.exports = {
	async up(queryInterface, Sequelize) {
		// const nearbylists = [];

		// await queryInterface.bulkInsert("nearbylists", nearbylists, {});
	},
	async down(queryInterface, Sequelize) {
		// await queryInterface.bulkDelete("nearbylists", null, {});
	},
};
